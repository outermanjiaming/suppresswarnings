package com.suppresswarnings.osgi.common.protocol;

public enum KEY {
	UID, Name, Passwd, Token, Invite, Invited, Account, Login, Register, Created, LastLogin, Valid, Exist, Limit,
	//new KEY append at last:
	;
}
