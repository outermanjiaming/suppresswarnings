package com.suppresswarnings.osgi.user;

public enum KEY {
	UID, Name, Passwd, Token, Invite, Invited, Account, Login, Register, Created, LastLogin, @Deprecated Valid, Exist, Limit,
	//new KEY append at last:
	Interaction, WX;
}
